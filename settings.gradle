rootProject.name = 'client-rag-demo'

include 'backend'
// Shared hexagonal ingestion logic (document domain model, ports, Tika/chunk/pgvector adapter)
// used by both backend (web upload) and sharepoint-batch (SharePoint polling) - see CLAUDE.md.
include 'ingestion-core'
// Standalone Spring Batch service that polls a SharePoint document library via Microsoft Graph
// and (re-)ingests changed files through ingestion-core. Deployed separately from backend.
include 'sharepoint-batch'
